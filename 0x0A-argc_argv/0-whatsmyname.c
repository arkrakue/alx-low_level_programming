#include "main.h"
#include <stdio.h>
/**
 * main - the input code of the program
 * @argc: the value of the agrv array
 * @argv: array of strings in the command line
 * Return: 0 for success
 */

int main(int argc, char *argv[])
{
	if (argc > 0)
		printf("%s\n", argv[0]);
	else
	{
	}
	return (0);
}
