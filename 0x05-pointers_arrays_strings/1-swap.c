#include "main.h"

/**
 * swap_int - interchange the values of two
 * integers using their pointers
 * @a: the pointer value of the 1st integer
 * @b: the pointer value of the 2nd integer
 * Return: void
 */

void swap_int(int *a, int *b)
{
	int c;

	c = *a;
	*n = *b;
	*b = c;
}
