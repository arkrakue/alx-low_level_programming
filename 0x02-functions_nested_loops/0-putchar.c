#include <unistd.h>
#include "main.h"

/**
 * main - the code entry of the program
 *Return: 0(Success)
 */

int main(void)
{	char put[9] = "_putchar";
	int a;

	for (a = 0; a < 9; a++)
	{	_putchar(put[a]);
		_putchar('\n');

	return (0);
	}

	return (0);
}
