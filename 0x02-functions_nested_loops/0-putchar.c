#include <unistd.h>
#include "main.h"

/**
 * main - the code entry of the program
 *Return: 0(Success)
 */

int main(void)
{	char put[8] = "_putchar";
	int a;

	for (a = 0; a < 8; a++)
	{	_putchar(put[a]);

	return (0);
	}
		_putchar('\n');

	return (0);
}
