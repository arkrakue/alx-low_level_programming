#include  "main.h"
/**
 * _abs - prints the abosolute of the number
 * @a: The input number as an integer.
 * Return: 0(Success)
 */
int _abs(int a)
{
	if (a < 0)
	{
		a = -a;
		return (a);
	}
	else
	{
		return (a);
	}
}
