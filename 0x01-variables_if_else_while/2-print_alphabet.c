#include <stdio.h>
/**
 * main - the main code entry
 * Return: 0 (Success)
 */
int main(void)
{	char small;
	for (small = 'a'; small <= 'z'; small++)
	{
		putchar(small);
	}  putchar('\n');
	return (0);
}
