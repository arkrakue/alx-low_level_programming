#include <stdio.h>
/**
 * main - the code entry point for the program
 * Return: 0 (Success)
 */
int main(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
	{
		putchar(a);
	}
		putchar('\n');
	return (0);
}
